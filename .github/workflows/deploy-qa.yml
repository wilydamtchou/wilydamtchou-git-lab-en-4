name: Deploy QA

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to deploy (ex: v2026.02.06-140530)"
        required: true

concurrency:
  group: deploy-qa-${{ github.event.inputs.tag }}
  cancel-in-progress: false

jobs:
  deploy_qa:
    runs-on: self-hosted

    steps:
      - name: Clean Git cache (if exists)
        run: |
          if [ -d "${{ github.workspace }}/.git" ]; then
            cd ${{ github.workspace }}
            git clean -fdx
            git reset --hard
            rm -rf .git
          fi
        continue-on-error: true

      - name: Checkout selected tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag }}
          fetch-depth: 1
          clean: true
          lfs: false

      - name: Show version
        run: git describe --tags --always

      - name: Setup Java with Maven cache
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - name: Build Maven package for tag
        run: mvn clean package -DskipTests -q -Dmaven.wagon.http.retryHandler.class=standard -Dmaven.wagon.http.retryHandler.count=5

      - name: Deploy to QA
        run: bash ./scripts/deploy-qa.sh

      - name: Verify QA Deployment
        run: |
          echo "⏳ Waiting for QA application to start..."
          sleep 20
          
          echo "🧪 Testing QA application with retries..."
          for i in {1..10}; do
            echo "  Attempt $i/10..."
            if curl -s http://localhost:8082/actuator/health > /dev/null 2>&1; then
              echo "✅ QA deployment successful!"
              exit 0
            fi
            sleep 3
          done
          
          echo "❌ QA application not responding after retries"
          echo ""
          echo "📋 Container logs:"
          docker compose -f docker-compose-qa.yml logs
          exit 1
