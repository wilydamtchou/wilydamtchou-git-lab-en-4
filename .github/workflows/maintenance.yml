name: Maintenance

on:
  schedule:
    - cron: "*/15 * * * *"  # ✅ Execution every 15 minutes, every day
  workflow_dispatch:         # Possibility to trigger manually

jobs:
  maintenance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cleanup
        run: ./scripts/cleanup.sh